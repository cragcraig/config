Configuration files for vim, tmux, inputrc, etc. Also includes some useful scripts in ~/bin/.

Just clone the repo, "cp -r *" to your $HOME directory, and run :BundleInstall in vim.


DEPENDENCIES:

Vim 7.4
tmux 1.8
